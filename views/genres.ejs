<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Page</title>
  </head>
  <body>
    "Hello this is the books by genre" 

    <ul>
      <% booksByGenre.map((record) => { %>
        <li>
           Title:
          "<a href="/item/<%= record.book_id %>">
          <%= record.title %>
         </a>" ||

           
          Pages: <%= record.pages %> ||

   

                  </li>
      <% }) %>
  </ul>
  </body>
</html>
